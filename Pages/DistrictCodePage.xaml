<Page x:Class="DevTools.Pages.DistrictCodePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:resources="clr-namespace:DevTools.Resources"
      Title="DistrictCodePage">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Click="Back_Click" Grid.Row="0" HorizontalAlignment="Left" Margin="0,0,0,12" Width="40" Height="40" ToolTip="{x:Static resources:Strings.Back}">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="#4A4A4A" CornerRadius="20" Width="40" Height="40" SnapsToDevicePixels="True">
                        <TextBlock FontFamily="{StaticResource FontAwesomeSolid}" Text="&#xf060;" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFFFFF" />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <TextBlock Grid.Row="0" Text="{x:Static resources:Strings.PageDistrictCode}" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,8" />

        <GroupBox Grid.Row="1" Header="{x:Static resources:Strings.SearchAddressOrCode}" Margin="0,10,0,10" Padding="15">
            <Grid Margin="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchInput" Grid.Column="0" Height="32" FontSize="14" />
                <Button Click="Search_Click" Grid.Column="1" Margin="8,0,0,0" Width="80" Height="32" ToolTip="{x:Static resources:Strings.Search}">
                    <TextBlock FontFamily="{StaticResource FontAwesomeSolid}" Text="&#xf002;" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" />
                </Button>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="{x:Static resources:Strings.SearchResults}" Margin="0,10,0,0" Padding="15">
            <ListView x:Name="ResultListView">
                <ListView.Resources>
                    <Style TargetType="TextBox" x:Key="NoBorderTextBoxStyle">
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Padding" Value="4,2" />
                        <Setter Property="FontSize" Value="13" />
                        <Setter Property="BorderBrush" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                        <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.Resources>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="{x:Static resources:Strings.AddressName}" Width="450">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding DisplayName, Mode=OneWay}" IsReadOnly="True" Style="{StaticResource NoBorderTextBoxStyle}" MouseDoubleClick="TextBox_MouseDoubleClick" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="{x:Static resources:Strings.DistrictCode}" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Code, Mode=OneWay}" IsReadOnly="True" Style="{StaticResource NoBorderTextBoxStyle}" MouseDoubleClick="TextBox_MouseDoubleClick" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>
    </Grid>
</Page>
