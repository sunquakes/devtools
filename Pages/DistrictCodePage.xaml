<Page x:Class="DevTools.Pages.DistrictCodePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="DistrictCodePage">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Click="Back_Click" Grid.Row="0" HorizontalAlignment="Left" Margin="0,0,0,12" Width="40" Height="40" ToolTip="返回">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="#4A4A4A" CornerRadius="20" Width="40" Height="40" SnapsToDevicePixels="True">
                        <TextBlock FontFamily="{StaticResource FontAwesomeSolid}" Text="&#xf060;" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFFFFF" />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <TextBlock Grid.Row="0" Text="行政编码查询" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,8" />

        <GroupBox Grid.Row="1" Header="输入地址名称或行政编码进行搜索" Margin="0,10,0,10" Padding="15">
            <Grid Margin="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchInput" Grid.Column="0" Height="32" FontSize="14" />
                <Button Click="Search_Click" Grid.Column="1" Margin="8,0,0,0" Width="80" Height="32" Content="搜索" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="搜索结果" Margin="0,10,0,0" Padding="15">
            <ListView x:Name="ResultListView">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="地址名称" Width="450">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding DisplayName, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="4,2" FontSize="13" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="行政编码" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Code, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="4,2" FontSize="13" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>
    </Grid>
</Page>
