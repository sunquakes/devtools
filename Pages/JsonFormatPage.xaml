<Page x:Class="DevTools.Pages.JsonFormatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:resources="clr-namespace:DevTools.Resources"
      Title="JsonFormatPage">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Click="Back_Click" HorizontalAlignment="Left" Margin="0,0,0,8" Width="40" Height="40" ToolTip="{x:Static resources:Strings.Back}">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="#4A4A4A" CornerRadius="20" Width="40" Height="40" SnapsToDevicePixels="True">
                        <TextBlock FontFamily="{StaticResource FontAwesomeSolid}" Text="&#xf060;" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFFFFF" />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <TextBlock Grid.Row="1" Text="{x:Static resources:Strings.PageJSONFormat}" FontSize="18" Margin="0,10" />
        <TextBox Grid.Row="2" x:Name="InputText" Height="150" TextWrapping="Wrap" AcceptsReturn="True" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10">
            <Button Click="Format_Click" Width="80" Height="32" ToolTip="{x:Static resources:Strings.Format}">
                <TextBlock FontFamily="{StaticResource FontAwesomeSolid}" Text="&#xf121;" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" />
            </Button>
            <Button Click="Compress_Click" Margin="8,0,0,0" Width="100" Height="32" ToolTip="JSON 压缩">
                <TextBlock FontFamily="{StaticResource FontAwesomeSolid}" Text="&#xf066;" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" />
            </Button>
            <Button Click="ExpandAll_Click" Margin="8,0,0,0" Width="100" Height="32" ToolTip="{x:Static resources:Strings.ExpandAll}">
                <TextBlock FontFamily="{StaticResource FontAwesomeSolid}" Text="&#xf078;" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" />
            </Button>
            <Button Click="CollapseAll_Click" Margin="8,0,0,0" Width="100" Height="32" ToolTip="{x:Static resources:Strings.CollapseAll}">
                <TextBlock FontFamily="{StaticResource FontAwesomeSolid}" Text="&#xf077;" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" />
            </Button>
            <Button Click="CopyFormatted_Click" Margin="8,0,0,0" Width="120" Height="32" ToolTip="{x:Static resources:Strings.CopyFormatted}">
                <TextBlock FontFamily="{StaticResource FontAwesomeSolid}" Text="&#xf0c5;" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" />
            </Button>
        </StackPanel>

        <!-- Collapsible formatted JSON display -->
        <Border Grid.Row="4" BorderBrush="LightGray" BorderThickness="1" Padding="4">
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="JsonOutputPanel" />
                </ScrollViewer>
                <Border x:Name="LoadingOverlay" Background="#80000000" Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="处理中..." FontSize="16" Foreground="White" HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>